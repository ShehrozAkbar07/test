import 'dart:ui';

import 'package:flutter/material.dart';

class AppColors {
  static const primary = Color(0xff091521);
  static const white = Color(0xffffffff);
  static const grey = Colors.grey;
}
